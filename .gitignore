node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
*.log

# Course materials live in the parent folder and must never be committed:
# copyrighted textbooks, instructor solution manuals, exam keys.
*.pdf
*.pptx
*.docx
/Homeworks/
/textbooks/
/student notes from past years/

# local screenshot scratch
shot*.mjs

# Private maintainer notes, kept in a separate private repo cloned to this
# path. Nothing under notes/ is ever published: it holds the authoritative
# working-notes document, the course-specific planning map (which references
# homework/exam content and reserved in-class demos), and scratch scripts.
# Setup instructions are in CLAUDE.md.
/notes/

# Belt and braces: the course map must never be committed here even if it is
# moved or copied back out of notes/.
*course-map.md

# Local Claude Code tooling config
.claude/
